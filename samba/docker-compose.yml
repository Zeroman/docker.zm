version: '2'

services:
  samba:
    restart: always
    image: dperson/samba
    volumes:
    - /tmp/.samba:/temp
    - /work/temp:/share
    ports:
    - "139:139"
    - "445:445"
    command: -s "temp;/temp;yes;no;yes" -s "share;/share"
